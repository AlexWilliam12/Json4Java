package no.foundation.serializer;

import no.foundation.serializer.tree.JsonNode;

class JsonPrinter {

    public String print(JsonNode node, boolean formatted) {
        if (formatted) {

        }
        return node.toString();
    }
}
